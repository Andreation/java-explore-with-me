CREATE TABLE IF NOT EXISTS hits
(
    hit_id      BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    hit_app     VARCHAR(200)  NOT NULL,
    hit_uri     VARCHAR(2000) NOT NULL,
    hit_ip      VARCHAR(20)   NOT NULL,
    hit_created TIMESTAMP
);